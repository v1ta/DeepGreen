<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:gravity="center"
    android:background="@drawable/wood"
    tools:context="com.defritza.app.GameSelect" >
    
    <ImageView
        android:id="@+id/knightView"
        android:src="@drawable/knight"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/knight_desc" />
        
    <Button
        android:id="@+id/localButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dip"
        android:layout_marginBottom="15dip"
        android:text="@string/button_local_game"
        android:background="@drawable/rounded_rect" />
    <Button 
        android:id="@+id/remoteButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dip"
        android:layout_marginBottom="15dip"
        android:layout_below="@id/localButton"
        android:text="@string/button_bt_game"
        android:enabled="false"
        android:background="@drawable/rounded_rect" />
    <Button 
        android:id="@+id/computerButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dip"
        android:layout_marginBottom="15dip"
        android:layout_below="@id/remoteButton"
        android:text="@string/button_computer_game"
        android:background="@drawable/rounded_rect" />
    <Button 
        android:id="@+id/historyButton"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dip"
        android:layout_below="@id/computerButton"
        android:text="@string/button_replay_game"
        android:background="@drawable/rounded_rect" />
    
</RelativeLayout>
